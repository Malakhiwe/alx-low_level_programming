#include "main.h"
/**
 * _isdigit - Function check for a digit
 * @c:  We check this variable in the function
 * Return: 1 if c is a digit otherwise 0
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
