#include "main.h"
#include <stdio.h>

/**
 * main - This function print the name of the program.
 * @argc: This parm contains the number of arguments.
 * @argv: Contains the number of arguments.
 * Return: 0 Success.
 */

int main(int argc __attribute__ ((unused)), char *argv[])
{

printf("%s\n", *argv);

return (0);
}
