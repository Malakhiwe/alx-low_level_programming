#include <stdio.h>
/**
 * main - Entry into the program
 * Description: Prints all the numbers of bas 16
 * Return: 0 Success
 **/
int main(void)
{
char n;
for (n = 0; n <= 16; n++)
{
putchar(n);
}
putchar('\n');
return (0);
}
